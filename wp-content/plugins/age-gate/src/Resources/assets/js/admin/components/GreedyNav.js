import priorityNav from "priority-nav";

const nav = priorityNav.init({
    mainNavWrapper: '.age-gate-toolbar',
    navDropdownClassName: 'age-gate-toolbar__subnav',
    navDropdownToggleClassName: 'age-gate-toolbar__subnav-toggle',
    breakPoint: 0,
});
